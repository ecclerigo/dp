To install and run:

* Ensure that Node is installed (or obtain from https://nodejs.org) and available on path
* Run "npm install" from root directory
* Navigate to the webservice directory and run "node webservice.js" in a terminal window
* Open another terminal window and run "node app.js" in the root directory
* Open a web browser to http://localhost:8000 to view application